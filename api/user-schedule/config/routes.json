{
  "routes": [
    {
      "method": "GET",
      "path": "/user-schedules",
      "handler": "user-schedule.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/user-schedules/count",
      "handler": "user-schedule.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/user-schedules/:id",
      "handler": "user-schedule.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/user-schedules",
      "handler": "user-schedule.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/user-schedules/:id",
      "handler": "user-schedule.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/user-schedules/:id",
      "handler": "user-schedule.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
